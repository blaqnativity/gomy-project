import { ClipLoader } from "react-spinners";

const Loader = () => (
  <div className="max-w-3xl mx-auto flex justify-center items-center mt-20">
    <ClipLoader size={100} />
  </div>
);

export default Loader;
